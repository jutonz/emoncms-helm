version: 2
jobs:
  lint:
    working_directory: ~/emoncms
    docker:
    - image: lachlanevenson/k8s-helm
    steps:
    - checkout
    - run: helm lint .

  package:
    working_directory: ~/emoncms
    docker:
    - image: lachlanevenson/k8s-helm
    steps:
    - checkout
    - run: helm package .

workflows:
  version: 2
  lint:
    jobs:
    - lint
    - package:
        requires: ["lint"]
